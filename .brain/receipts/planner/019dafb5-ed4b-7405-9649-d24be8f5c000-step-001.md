---
agent: planner
chain_id: 019dafb5-ed4b-7405-9649-d24be8f5c000
step: 1
verdict: completed
---

1. **Chosen improvement:** Add a keyboard-accessible “Skip to content” link plus a stable `id` target on the main content container in `src/App.tsx`.
2. **Why it is high-value:** The app shell currently has persistent nav/chrome but no skip link or main target; this is a fast, low-risk accessibility win for keyboard and screen-reader users.
3. **Likely touched files:** `src/App.tsx`; optional `src/App.test.tsx` (new); optional `src/test/setup.ts` only if lightweight mocks are needed.
4. **Tests to add/update:** Add one RTL/Vitest app-shell test that renders `App` under `MemoryRouter` and asserts the skip link exists, points to `#main-content`, and the `<main>` landmark has that id (and optionally `tabIndex={-1}` if used).
5. **Recommended auditors:** correctness-auditor; accessibility/UX reviewer.
